<p id="notice"><%= notice %></p>

<h1><%= t('Articles') %></h1>
<table class='table table-striped table-hover'>
  <thead>
    <tr>
      <th><%= t('user') %></th>
      <th><%= t('category') %></th>
      <th>Photo</th>
      <th><%= t('content') %></th>
     　<th><%= t('address') %></th>
    </tr>
  </thead>

  <tbody>
    <% @articles.each do |article| %>
      <tr>
        <td><%= link_to article.user.name, article, class: 'widelink' %></td>
        <td><%= link_to article.category.name, article, class: 'widelink' %></td>
        <td><%= link_to attachment_image_tag article, :image, :fill, 80, 80, format: 'jpeg', class: 'widelink' %></td>
        <td><%= link_to article.content, article, class: 'widelink' %></td>
        <td><%= link_to article.address, article, class: 'widelink' %></td>
        <!--<td><%= link_to 'Show', article %></td>-->
        <!--<% if user_signed_in? && article.user_id == current_user.id %>-->

        <!--<% end %>-->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Article', new_article_path, class: 'btn btn-primary' %>
<%= link_to 'Show Categories', categories_path %>
